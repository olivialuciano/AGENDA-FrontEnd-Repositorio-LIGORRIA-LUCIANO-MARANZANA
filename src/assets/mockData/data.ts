import { Contact, defaultContact } from "src/app/core/interfaces/contact";

export const contactData:Contact[] = [
    defaultContact,
    defaultContact,
    defaultContact,
    defaultContact,
    defaultContact,
    defaultContact,
]